// you can write to stdout for debugging purposes, e.g.
// print "this is a debug message\n";

function solution($A, $K) {
    // write your code in PHP7.0
    for($i=0; $i< $K;$i++){
      array_unshift($A,$A[sizeof($A)-1]);
      array_pop($A);
      
    }
    return $A;
}
